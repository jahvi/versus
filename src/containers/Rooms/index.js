import React from 'react';
import Logout from '../../components/Logout';

function Rooms() {
  return (
    <div>
      Rooms Wrapper
      <Logout>Logout</Logout>
    </div>
  );
}

export default Rooms;
